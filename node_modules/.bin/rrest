#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rrestjs/bin/rrest" "$@"
  ret=$?
else 
  node  "$basedir/../rrestjs/bin/rrest" "$@"
  ret=$?
fi
exit $ret
